pipeline {
    agent {
        dockerfile true
    }
    stages {
        stage('Prepare website') {
            steps {
                sh 'pwd'
                sh 'ls -al .'
            }
        }
        
    }
}
